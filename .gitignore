# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
.env
.env.*
!.env.example
dist-ssr
*.local

# Test artifacts
test-results
playwright-report

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# tesseract runtime assets - restored from node_modules by scripts/vendor-tesseract.mjs
# (postinstall). Only eng.traineddata.gz is committed; it has no npm source.
public/tesseract/worker.min.js
public/tesseract/tesseract-core-simd-lstm.wasm
public/tesseract/tesseract-core-simd-lstm.wasm.js

# README screenshot written by the repo-audit skill
docs/screenshots/

# Board originals written by scripts/extract-board-images.mjs before it rewrites a
# board. They contain the FULL inline base64 of every image that was on it - 10 MB
# of board content that must never reach git history. Local safety net only.
backups/
