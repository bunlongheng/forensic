// Single source of truth for the app version shown in the UI. Bump this on every
// deploy (v1 -> v2 -> ...) so you can confirm at a glance which build is live: if
// the screen shows an older number than expected, the deploy did not go through.
export const VERSION = 'v27'
